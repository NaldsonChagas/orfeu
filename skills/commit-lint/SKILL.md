# Skill: commit-lint

**When to load:** When writing a commit message.

**What it does:** Validates Conventional Commits format, suggests corrections.

## Expected pattern

```
<type>(<scope>): <description in English>
```

**Valid types:** feat, fix, chore, docs, refactor, test, style, perf, ci, build, revert